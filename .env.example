# agentic-os reads model endpoints from config.yaml; secrets stay in the environment.
AGENTIC_OS_CONFIG=config.yaml
AGENTIC_OS_HOME=.agentic-os
# Premium / cheap tier keys (only used if a task escalates to those tiers):
# KIMI_API_KEY=...
# OPENAI_API_KEY=...
